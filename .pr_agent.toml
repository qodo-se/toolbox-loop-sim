# Standing gate demo: the fixer is ON here so Qodo's proposed remediation is
# visible alongside the blocked PR. Main keeps it OFF so the toolbox loop is
# the only fixer there.
[fixer_agent]
enable_auto_fix = true
open_new_pr = true
